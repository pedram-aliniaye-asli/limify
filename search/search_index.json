{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Limify","text":"<p>Limify is a Redis-backed, framework-agnostic rate limiting engine for Python.</p> <p>It provides a clean architecture core with pluggable adapters, async and sync execution, and atomic Redis Lua algorithms.</p> <p>Limify is designed for:</p> <ul> <li>Backend engineers</li> <li>API platforms</li> <li>Microservices architectures</li> <li>SaaS platforms with multi-tier limits</li> </ul>"},{"location":"#key-features","title":"Key Features","text":"<ul> <li>Framework-agnostic core</li> <li>Async and sync Redis support</li> <li>Atomic Redis Lua execution</li> <li>Rule-based rate limiting</li> <li>Plan-based rate limiting</li> <li>Multiple identity types</li> <li>Clean architecture design</li> <li>Pluggable algorithms and storage</li> </ul>"},{"location":"#example","title":"Example","text":"<pre><code>from limify import Limify, LimifyConfig\n\nconfig = LimifyConfig(\n    rules=[\n        {\n            \"id\": \"items\",\n            \"method\": \"*\",\n            \"path\": \"/items\",\n            \"rate\": \"10/minute\",\n        }\n    ]\n)\n</code></pre> <p>Use the navigation menu to explore the documentation.</p>"},{"location":"core-concepts/architecture/","title":"Architecture","text":"<p>Limify follows a layered clean architecture that separates framework code, rate-limiting logic, and storage concerns.</p> <p>This allows Limify to remain framework-agnostic, highly testable, and easily extensible.</p> <pre><code>Application (FastAPI / etc.)  \n        \u2193  \nAdapter (Middleware)  \n        \u2193  \nLimiter (Orchestrator)  \n        \u2193  \nResolvers  \n  \u251c\u2500 RuleResolver  \n  \u251c\u2500 PlanResolver  \n  \u2514\u2500 KeyResolver  \n        \u2193  \nAlgorithm (Token Bucket)  \n        \u2193  \nStorage Adapter (Redis)  \n</code></pre> <p>Each layer has a specific responsibility and communicates only with the layer directly below it.</p>"},{"location":"core-concepts/architecture/#application-layer","title":"Application Layer","text":"<p>The application layer represents the host framework.</p> <p>Examples:</p> <ul> <li>FastAPI</li> <li>Starlette</li> <li>Flask</li> <li>background workers</li> <li>API gateways</li> </ul> <p>Applications define endpoints and attach Limify middleware to enforce rate limits.</p> <p>Limify does not depend on any framework directly.</p>"},{"location":"core-concepts/architecture/#adapter-layer-middleware","title":"Adapter Layer (Middleware)","text":"<p>Adapters integrate Limify with web frameworks.</p> <p>For example:</p> <pre><code>limify.adapters.fastapi.middleware.LimifyMiddleware\n</code></pre> <p>Responsibilities:</p> <ul> <li>Intercept incoming requests</li> <li>Extract request metadata</li> <li>Construct a <code>RequestContext</code></li> <li>Call the Limiter</li> <li>Return HTTP 429 if the request is blocked</li> <li>Attach rate limit headers to responses</li> </ul> <p>This layer translates framework objects \u2192 Limify core objects.</p>"},{"location":"core-concepts/architecture/#limiter-orchestrator","title":"Limiter (Orchestrator)","text":"<p>The <code>Limiter</code> is the central component that coordinates the rate-limiting process.</p> <p>Responsibilities:</p> <ol> <li>Resolve the matching rule</li> <li>Resolve the active plan</li> <li>Resolve the identity key</li> <li>Execute the algorithm</li> <li>Return the rate-limit result</li> </ol> <p>The limiter itself contains no framework logic and no storage logic.</p> <p>It only orchestrates the core components.</p>"},{"location":"core-concepts/architecture/#resolvers","title":"Resolvers","text":"<p>Resolvers determine how requests map to rate limits.</p> <p>They are responsible for interpreting request metadata.</p>"},{"location":"core-concepts/architecture/#ruleresolver","title":"RuleResolver","text":"<p>Determines which rule applies to the request.</p> <p>Rules are matched based on:</p> <ul> <li>HTTP method</li> <li>request path</li> <li>wildcard patterns</li> <li>priority order</li> </ul> <p>Example rule:</p> <pre><code>{\n    \"id\": \"items\",\n    \"method\": \"*\",\n    \"path\": \"/items\",\n    \"rate\": \"10/minute\",\n    \"priority\": 10,\n}\n</code></pre> <p>Rules with higher priority override lower priority ones.</p>"},{"location":"core-concepts/architecture/#planresolver","title":"PlanResolver","text":"<p>Determines which plan applies to the request.</p> <p>Plans allow Limify to support multi-tier SaaS limits.</p> <p>Examples:</p> <pre><code>free \u2192 10/minute\npro \u2192 100/minute\nenterprise \u2192 1000/minute\n</code></pre> <p>The resolver may obtain plans from:</p> <ul> <li>a custom <code>PlanProvider</code></li> <li>the rule's rate definition</li> </ul> <p>If no custom plan exists, Limify derives a plan from the rule rate.</p>"},{"location":"core-concepts/architecture/#keyresolver","title":"KeyResolver","text":"<p>Determines the identity key used for rate limiting.</p> <p>Limify supports multiple identity types.</p> <p>Default resolution order:</p> <ol> <li><code>user_id</code></li> <li><code>org_id</code></li> <li><code>api_key</code></li> <li><code>client_ip</code></li> <li>anonymous</li> </ol> <p>The final Redis key format:</p> <pre><code>limify:{rule_id}:{plan_id}:{identity_type}:{identity_value}\n</code></pre> <p>Example:</p> <pre><code>limify:items:default:user:42\n</code></pre> <p>Each identity receives its own independent rate-limit bucket.</p>"},{"location":"core-concepts/architecture/#algorithm-layer","title":"Algorithm Layer","text":"<p>The algorithm implements the rate-limiting logic.</p> <p>Currently implemented:</p> <ul> <li>Token Bucket (sync)</li> <li>Token Bucket (async)</li> </ul> <p>The algorithm:</p> <ol> <li>Reads the current token state</li> <li>Refills tokens based on elapsed time</li> <li>Consumes tokens for the request</li> <li>Determines if the request is allowed</li> </ol> <p>Algorithms operate purely on numeric values:</p> <pre><code>limit\nperiod_seconds\n</code></pre> <p>They are independent of request context and framework code.</p>"},{"location":"core-concepts/architecture/#storage-adapter-layer","title":"Storage Adapter Layer","text":"<p>The storage adapter provides persistent state for algorithms.</p> <p>Currently supported:</p> <ul> <li><code>RedisAsyncAdapter</code></li> <li><code>RedisSyncAdapter</code></li> </ul> <p>Storage adapters handle:</p> <ul> <li>loading Lua scripts</li> <li>executing scripts</li> <li>interacting with Redis</li> </ul>"},{"location":"core-concepts/architecture/#redis-lua-execution","title":"Redis Lua Execution","text":"<p>Limify executes rate-limit logic using Redis Lua scripts.</p> <p>Benefits:</p> <ul> <li>atomic execution</li> <li>no race conditions</li> <li>safe in distributed systems</li> <li>consistent across multiple workers</li> </ul> <p>The Lua script:</p> <ul> <li>calculates token refill</li> <li>updates bucket state</li> <li>returns remaining tokens</li> </ul> <p>This guarantees correct behavior even under high concurrency.</p>"},{"location":"core-concepts/architecture/#why-this-architecture-matters","title":"Why This Architecture Matters","text":"<p>This architecture enables:</p>"},{"location":"core-concepts/architecture/#framework-independence","title":"Framework independence","text":"<p>Limify can integrate with any Python framework.</p>"},{"location":"core-concepts/architecture/#testability","title":"Testability","text":"<p>Core logic can be tested without Redis or frameworks.</p>"},{"location":"core-concepts/architecture/#extensibility","title":"Extensibility","text":"<p>New algorithms or storage systems can be added easily.</p>"},{"location":"core-concepts/architecture/#maintainability","title":"Maintainability","text":"<p>Each component has a single responsibility.</p>"},{"location":"core-concepts/architecture/#summary","title":"Summary","text":"<p>The Limify architecture separates concerns into independent layers:</p> <ul> <li>adapters integrate frameworks</li> <li>resolvers determine rules and identities</li> <li>the limiter orchestrates execution</li> <li>algorithms enforce rate limits</li> <li>storage adapters persist state</li> </ul> <p>This design makes Limify suitable for high-performance distributed systems and modern backend platforms.</p>"},{"location":"core-concepts/configuration/","title":"Configuration","text":"<p>Limify uses <code>LimifyConfig</code> to define rules.</p> <p>Example:</p> <pre><code>config = LimifyConfig(\n    rules=[\n        {\n            \"id\": \"items\",\n            \"method\": \"*\",\n            \"path\": \"/items\",\n            \"rate\": \"10/minute\",\n            \"priority\": 10,\n        }\n    ]\n)\n</code></pre> <p>Higher priority rules override lower priority rules.</p>"},{"location":"core-concepts/identity-resolution/","title":"Identity Resolution","text":"<p>Identity resolution determines who the rate limit applies to.</p> <p>Instead of limiting requests globally, Limify isolates rate limits per identity.</p> <p>This ensures that one user or client cannot consume the entire rate limit for everyone else.</p>"},{"location":"core-concepts/identity-resolution/#identity-priority","title":"Identity Priority","text":"<p>When a request arrives, Limify determines the request identity using the following priority order:</p> <ol> <li><code>user_id</code></li> <li><code>org_id</code></li> <li><code>api_key</code></li> <li><code>client_ip</code></li> <li><code>anonymous</code></li> </ol> <p>The first available identity is used.</p> <p>Example request context:</p> <pre><code>RequestContext(\n    method=\"GET\",\n    path=\"/items\",\n    user_id=42,\n    org_id=10,\n    api_key=None,\n    client_ip=\"127.0.0.1\"\n)\n</code></pre> <p>In this case:</p> <pre><code>user_id = 42\n</code></pre> <p>So the identity used will be:</p> <pre><code>user:42\n</code></pre> <p>Even though <code>org_id</code> and <code>client_ip</code> exist, they are ignored because <code>user_id</code> has higher priority.</p>"},{"location":"core-concepts/identity-resolution/#why-identity-priority-exists","title":"Why Identity Priority Exists","text":"<p>Different applications identify clients differently.</p> <p>For example:</p> Scenario Best Identity Authenticated user <code>user_id</code> Multi-tenant SaaS <code>org_id</code> Public API <code>api_key</code> Anonymous traffic <code>client_ip</code> <p>The priority system allows Limify to automatically choose the most specific identity available.</p>"},{"location":"core-concepts/identity-resolution/#redis-key-structure","title":"Redis Key Structure","text":"<p>Once an identity is selected, Limify constructs a deterministic Redis key.</p> <p>Format:</p> <pre><code>limify:{rule_id}:{plan_id}:{identity_type}:{identity_value}\n</code></pre> <p>Example keys:</p> <pre><code>limify:items:default:user:42\nlimify:items:pro:apikey:abc123\nlimify:default:default:ip:127.0.0.1\n</code></pre> <p>This ensures that each identity has a separate token bucket.</p>"},{"location":"core-concepts/identity-resolution/#independent-rate-limit-buckets","title":"Independent Rate Limit Buckets","text":"<p>Each identity receives its own rate limit bucket.</p> <p>Example rule:</p> <pre><code>10/minute\n</code></pre> <p>Requests:</p> <pre><code>User 1 \u2192 10/minute\nUser 2 \u2192 10/minute\nUser 3 \u2192 10/minute\n</code></pre> <p>User 1 exhausting their quota does not affect other users.</p>"},{"location":"core-concepts/identity-resolution/#example-scenario","title":"Example Scenario","text":"<p>Rule:</p> <pre><code>10/minute\n</code></pre> <p>Requests:</p> <pre><code>User A \u2192 10 requests allowed\nUser B \u2192 10 requests allowed\nUser C \u2192 10 requests allowed\n</code></pre> <p>Each user has their own bucket:</p> <pre><code>limify:items:default:user:A\nlimify:items:default:user:B\nlimify:items:default:user:C\n</code></pre>"},{"location":"core-concepts/identity-resolution/#anonymous-traffic","title":"Anonymous Traffic","text":"<p>If no identity is provided in the request context, Limify falls back to the client IP.</p> <p>Example:</p> <pre><code>client_ip = 192.168.1.10\n</code></pre> <p>Redis key:</p> <pre><code>limify:default:default:ip:192.168.1.10\n</code></pre> <p>This allows Limify to rate limit anonymous clients.</p>"},{"location":"core-concepts/identity-resolution/#requestcontext","title":"RequestContext","text":"<p>Identity resolution uses the <code>RequestContext</code> object.</p> <p>Example:</p> <pre><code>RequestContext(\n    method=\"GET\",\n    path=\"/items\",\n    user_id=42,\n    org_id=None,\n    api_key=None,\n    client_ip=\"127.0.0.1\"\n)\n</code></pre> <p>Framework adapters (like FastAPI middleware) are responsible for constructing this context.</p>"},{"location":"core-concepts/identity-resolution/#why-this-design-matters","title":"Why This Design Matters","text":"<p>This design enables:</p>"},{"location":"core-concepts/identity-resolution/#multi-tenant-saas-platforms","title":"Multi-tenant SaaS platforms","text":"<p>Organizations receive independent rate limits.</p>"},{"location":"core-concepts/identity-resolution/#user-based-rate-limiting","title":"User-based rate limiting","text":"<p>Authenticated users are isolated from each other.</p>"},{"location":"core-concepts/identity-resolution/#public-apis","title":"Public APIs","text":"<p>API keys can be rate limited independently.</p>"},{"location":"core-concepts/identity-resolution/#anonymous-traffic-control","title":"Anonymous traffic control","text":"<p>IP-based limits protect services from abuse.</p>"},{"location":"core-concepts/identity-resolution/#summary","title":"Summary","text":"<p>Identity resolution determines who the rate limit applies to.</p> <p>Limify selects the identity using the following priority:</p> <pre><code>user_id \u2192 org_id \u2192 api_key \u2192 client_ip \u2192 anonymous\n</code></pre> <p>Each identity produces its own Redis key and receives an independent rate limit bucket.</p>"},{"location":"core-concepts/plans/","title":"Plans","text":"<p>Limify supports plan-based rate limiting, which makes it suitable for SaaS platforms, APIs, and multi-tier systems.</p> <p>A plan defines the numeric rate limit that will actually be enforced for a request.</p> <p>Example plans:</p> <pre><code>free       \u2192 10/minute\npro        \u2192 100/minute\nenterprise \u2192 1000/minute\n</code></pre> <p>This allows different users, organizations, or API consumers to receive different rate limits based on subscription tier or account type.</p>"},{"location":"core-concepts/plans/#why-plans-exist","title":"Why Plans Exist","text":"<p>Rules determine:</p> <ul> <li>which requests match</li> <li>which endpoints are affected</li> <li>the default rate definition</li> </ul> <p>Plans determine:</p> <ul> <li>the actual limit applied to the request</li> <li>the time period used by the algorithm</li> <li>the plan identity included in the Redis key</li> </ul> <p>This separation is important because two requests may match the same rule but still receive different limits depending on the user or organization plan.</p> <p>For example:</p> <ul> <li><code>GET /items</code> by a free user \u2192 <code>10/minute</code></li> <li><code>GET /items</code> by a pro user \u2192 <code>100/minute</code></li> </ul> <p>Same endpoint, same rule, different plan.</p>"},{"location":"core-concepts/plans/#plan-structure","title":"Plan Structure","text":"<p>Internally, a plan contains:</p> <ul> <li><code>id</code></li> <li><code>limit</code></li> <li><code>period_seconds</code></li> </ul> <p>Example:</p> <pre><code>Plan(\n    id=\"pro\",\n    limit=100,\n    period_seconds=60,\n)\n</code></pre> <p>This means:</p> <ul> <li>plan name = <code>pro</code></li> <li>100 requests allowed</li> <li>every 60 seconds</li> </ul> <p>The algorithm operates on these numeric values.</p>"},{"location":"core-concepts/plans/#relationship-between-rules-and-plans","title":"Relationship Between Rules and Plans","text":"<p>A rule contains a human-readable rate string such as:</p> <pre><code>{\n    \"id\": \"items\",\n    \"method\": \"*\",\n    \"path\": \"/items\",\n    \"rate\": \"10/minute\",\n}\n</code></pre> <p>If no custom plan provider is used, Limify derives a default plan from the rule rate.</p> <p>Example:</p> <pre><code>10/minute\n</code></pre> <p>becomes:</p> <pre><code>Plan(\n    id=\"default\",\n    limit=10,\n    period_seconds=60,\n)\n</code></pre> <p>So the rule provides the fallback rate limit.</p>"},{"location":"core-concepts/plans/#default-behavior","title":"Default Behavior","text":"<p>If no custom <code>PlanProvider</code> returns a plan, Limify automatically creates a plan from the rule\u2019s <code>rate</code>.</p> <p>Example:</p> <p>Rule:</p> <pre><code>{\n    \"id\": \"items\",\n    \"method\": \"*\",\n    \"path\": \"/items\",\n    \"rate\": \"10/minute\",\n}\n</code></pre> <p>Derived plan:</p> <pre><code>Plan(\n    id=\"default\",\n    limit=10,\n    period_seconds=60,\n)\n</code></pre> <p>This makes plans optional for simple use cases.</p>"},{"location":"core-concepts/plans/#custom-planprovider","title":"Custom PlanProvider","text":"<p>For SaaS or multi-tenant systems, you can provide your own <code>PlanProvider</code>.</p> <p>A custom provider can decide the plan dynamically based on:</p> <ul> <li>user ID</li> <li>organization ID</li> <li>API key</li> <li>database records</li> <li>billing tier</li> <li>feature flags</li> </ul> <p>Example interface:</p> <pre><code>class CustomPlanProvider:\n    def resolve(self, context, rule):\n        pass\n</code></pre> <p>The provider receives:</p> <ul> <li><code>context</code> \u2192 request metadata</li> <li><code>rule</code> \u2192 the matched rule</li> </ul> <p>It should return either:</p> <ul> <li>a <code>Plan</code></li> <li>or <code>None</code> to fall back to the rule-derived default plan</li> </ul>"},{"location":"core-concepts/plans/#example-planprovider","title":"Example PlanProvider","text":"<p>Below is a simple example of a custom provider:</p> <pre><code>from limify.core.plans import Plan\n\n\nclass CustomPlanProvider:\n    def resolve(self, context, rule):\n        # Example only: replace with database or service lookup\n        if context.user_id == 1:\n            return Plan(id=\"enterprise\", limit=1000, period_seconds=60)\n\n        if context.user_id == 2:\n            return Plan(id=\"pro\", limit=100, period_seconds=60)\n\n        return None\n</code></pre> <p>Behavior:</p> <ul> <li>user 1 \u2192 enterprise plan</li> <li>user 2 \u2192 pro plan</li> <li>everyone else \u2192 default plan derived from <code>rule.rate</code></li> </ul>"},{"location":"core-concepts/plans/#example-with-organization-based-plans","title":"Example With Organization-Based Plans","text":"<p>Plans are especially useful for multi-tenant SaaS systems.</p> <p>Example:</p> <pre><code>from limify.core.plans import Plan\n\n\nclass CustomPlanProvider:\n    def resolve(self, context, rule):\n        if context.org_id == \"acme\":\n            return Plan(id=\"enterprise\", limit=1000, period_seconds=60)\n\n        if context.org_id == \"startup\":\n            return Plan(id=\"pro\", limit=100, period_seconds=60)\n\n        return None\n</code></pre> <p>In this example:</p> <ul> <li>organization <code>acme</code> gets enterprise limits</li> <li>organization <code>startup</code> gets pro limits</li> <li>all others fall back to the rule-defined rate</li> </ul>"},{"location":"core-concepts/plans/#how-plans-affect-redis-keys","title":"How Plans Affect Redis Keys","text":"<p>Plans are included in the Redis key.</p> <p>Format:</p> <pre><code>limify:{rule_id}:{plan_id}:{identity_type}:{identity_value}\n</code></pre> <p>Examples:</p> <pre><code>limify:items:free:user:42\nlimify:items:pro:user:42\nlimify:items:enterprise:user:42\n</code></pre> <p>This is important because each plan gets its own independent bucket.</p> <p>If a user upgrades from <code>free</code> to <code>pro</code>, they will use a different Redis key.</p>"},{"location":"core-concepts/plans/#example-end-to-end-flow","title":"Example End-to-End Flow","text":"<p>Suppose the rule is:</p> <pre><code>{\n    \"id\": \"items\",\n    \"method\": \"*\",\n    \"path\": \"/items\",\n    \"rate\": \"10/minute\",\n}\n</code></pre> <p>And the request context is:</p> <pre><code>RequestContext(\n    method=\"GET\",\n    path=\"/items\",\n    user_id=2,\n    org_id=None,\n    api_key=None,\n    client_ip=\"127.0.0.1\",\n)\n</code></pre> <p>Custom provider:</p> <pre><code>if context.user_id == 2:\n    return Plan(id=\"pro\", limit=100, period_seconds=60)\n</code></pre> <p>Result:</p> <ul> <li>matched rule = <code>items</code></li> <li>resolved plan = <code>pro</code></li> <li>key = <code>limify:items:pro:user:2</code></li> <li>effective limit = <code>100/minute</code></li> </ul> <p>So even though the rule says <code>10/minute</code>, the custom plan overrides it.</p>"},{"location":"core-concepts/plans/#when-to-use-custom-plans","title":"When to Use Custom Plans","text":"<p>Use a custom <code>PlanProvider</code> when:</p> <ul> <li>users have subscription tiers</li> <li>organizations have different quotas</li> <li>API keys map to different products</li> <li>you want billing-based or account-based limits</li> </ul> <p>Use only rule-derived plans when:</p> <ul> <li>all traffic should have the same rate limit</li> <li>you want simple endpoint-based throttling</li> <li>there is no account or tier differentiation</li> </ul>"},{"location":"core-concepts/plans/#summary","title":"Summary","text":"<p>Plans define the effective numeric rate limit applied to a request.</p> <p>They allow Limify to support:</p> <ul> <li>subscription tiers</li> <li>organization-level quotas</li> <li>API products</li> <li>multi-tenant SaaS systems</li> </ul> <p>If no custom plan is provided, Limify derives a default plan from the matched rule\u2019s <code>rate</code>.</p> <p>A custom <code>PlanProvider</code> can override that behavior and return a plan dynamically based on request context.</p>"},{"location":"core-concepts/rate-format/","title":"Rate Format","text":"<p>Limify expresses rate limits using a human-readable format:</p> <pre><code>&lt;limit&gt;/&lt;unit&gt;\n</code></pre> <p>Example:</p> <pre><code>10/minute\n</code></pre> <p>This means 10 requests are allowed per minute.</p> <p>Internally, Limify converts this format into numeric values used by the rate-limiting algorithm.</p> <p>Example conversion:</p> <pre><code>10/minute \u2192 limit = 10, period_seconds = 60\n</code></pre>"},{"location":"core-concepts/rate-format/#syntax","title":"Syntax","text":"<p>A rate definition contains two parts:</p> <pre><code>limit / unit\n</code></pre>"},{"location":"core-concepts/rate-format/#limit","title":"limit","text":"<p>The maximum number of requests allowed during the specified time unit.</p> <p>Example:</p> <pre><code>5/minute\n</code></pre> <p>Limit = 5 requests</p>"},{"location":"core-concepts/rate-format/#unit","title":"unit","text":"<p>Defines the time window for the rate limit.</p> <p>Example:</p> <pre><code>5/minute\n</code></pre> <p>Unit = minute</p>"},{"location":"core-concepts/rate-format/#supported-units","title":"Supported Units","text":"<p>Limify supports several time units and their common abbreviations.</p> Unit Aliases Seconds second <code>second</code>, <code>seconds</code>, <code>sec</code>, <code>s</code> 1 minute <code>minute</code>, <code>minutes</code>, <code>min</code>, <code>m</code> 60 hour <code>hour</code>, <code>hours</code>, <code>h</code> 3600 day <code>day</code>, <code>days</code>, <code>d</code> 86400 <p>Examples of valid rate formats:</p> <pre><code>5/second\n5/sec\n5/s\n\n10/minute\n10/min\n10/m\n\n100/hour\n100/h\n\n1000/day\n1000/d\n</code></pre> <p>All of the above are interpreted correctly by Limify.</p>"},{"location":"core-concepts/rate-format/#internal-conversion","title":"Internal Conversion","text":"<p>During initialization, Limify converts rate strings into numeric values.</p> <p>Example:</p> <pre><code>10/minute\n</code></pre> <p>becomes:</p> <pre><code>limit = 10\nperiod_seconds = 60\n</code></pre> <p>These values are passed to the rate-limiting algorithm.</p> <p>Example arguments passed to Redis Lua:</p> <pre><code>ARGV[1] = limit\nARGV[2] = period_seconds\nARGV[3] = current timestamp\n</code></pre> <p>The Lua script calculates the refill rate:</p> <pre><code>refill_rate = limit / period_seconds\n</code></pre> <p>This determines how quickly tokens are replenished in the token bucket.</p>"},{"location":"core-concepts/rate-format/#example-rule","title":"Example Rule","text":"<pre><code>{\n    \"id\": \"items\",\n    \"method\": \"*\",\n    \"path\": \"/items\",\n    \"rate\": \"10/minute\",\n}\n</code></pre> <p>This rule allows 10 requests per minute to <code>/items</code>.</p>"},{"location":"core-concepts/rate-format/#invalid-rate-formats","title":"Invalid Rate Formats","text":"<p>The following formats are invalid:</p> <pre><code>10 per minute\n10m\nminute/10\n</code></pre> <p>Rates must always follow:</p> <pre><code>&lt;number&gt;/&lt;unit&gt;\n</code></pre>"},{"location":"core-concepts/rate-format/#best-practices","title":"Best Practices","text":""},{"location":"core-concepts/rate-format/#use-smaller-windows-for-apis","title":"Use smaller windows for APIs","text":"<p>Example:</p> <pre><code>10/second\n</code></pre> <p>This protects APIs from bursts.</p>"},{"location":"core-concepts/rate-format/#use-larger-windows-for-background-jobs","title":"Use larger windows for background jobs","text":"<p>Example:</p> <pre><code>1000/hour\n</code></pre> <p>This allows high throughput while preventing abuse.</p>"},{"location":"core-concepts/rate-format/#summary","title":"Summary","text":"<p>Rate limits in Limify use the format:</p> <pre><code>&lt;limit&gt;/&lt;unit&gt;\n</code></pre> <p>Example:</p> <pre><code>10/minute\n</code></pre> <p>Which internally becomes:</p> <pre><code>limit = 10\nperiod_seconds = 60\n</code></pre> <p>These values are then used by the Redis-backed token bucket algorithm to enforce request limits.</p>"},{"location":"core-concepts/rules/","title":"Rules","text":"<p>Rules define how rate limits are applied to incoming requests.</p> <p>Each rule specifies:</p> <ul> <li>which requests it applies to</li> <li>what rate limit should be enforced</li> <li>how rules are prioritized</li> </ul> <p>Rules are configured through <code>LimifyConfig</code>.</p> <p>Example rule:</p> <pre><code>{\n    \"id\": \"items\",\n    \"method\": \"*\",\n    \"path\": \"/items\",\n    \"rate\": \"10/minute\",\n    \"priority\": 10,\n}\n</code></pre>"},{"location":"core-concepts/rules/#rule-fields","title":"Rule Fields","text":""},{"location":"core-concepts/rules/#id","title":"id","text":"<p>Unique identifier for the rule.</p> <p>Example:</p> <pre><code>\"id\": \"items\"\n</code></pre> <p>The rule ID is used internally to construct rate-limit keys.</p> <p>Example Redis key:</p> <pre><code>limify:items:default:user:42\n</code></pre> <p>Using stable rule IDs helps with monitoring and debugging.</p>"},{"location":"core-concepts/rules/#method","title":"method","text":"<p>Defines which HTTP method the rule applies to.</p> <p>Example:</p> <pre><code>\"method\": \"GET\"\n</code></pre> <p>Supported values:</p> <ul> <li>specific HTTP method (<code>GET</code>, <code>POST</code>, etc.)</li> <li><code>\"*\"</code> for all methods</li> </ul> <p>Example:</p> <pre><code>\"method\": \"*\"\n</code></pre> <p>This rule will apply to all HTTP methods.</p>"},{"location":"core-concepts/rules/#path","title":"path","text":"<p>Defines the endpoint pattern the rule applies to.</p> <p>Example:</p> <pre><code>\"path\": \"/items\"\n</code></pre> <p>Paths are matched against the request path using pattern matching.</p>"},{"location":"core-concepts/rules/#wildcard-matching","title":"Wildcard Matching","text":"<p>Limify supports wildcard patterns to match multiple endpoints.</p>"},{"location":"core-concepts/rules/#single-segment-wildcard","title":"Single segment wildcard","text":"<pre><code>/items/*\n</code></pre> <p>Matches:</p> <pre><code>/items/1\n/items/abc\n/items/test\n</code></pre> <p>But does NOT match:</p> <pre><code>/items/1/edit\n</code></pre>"},{"location":"core-concepts/rules/#multi-segment-wildcard","title":"Multi-segment wildcard","text":"<pre><code>/api/**\n</code></pre> <p>Matches:</p> <pre><code>/api/users\n/api/items/1\n/api/v1/orders/123\n</code></pre> <p>The <code>**</code> wildcard matches any remaining path segments.</p>"},{"location":"core-concepts/rules/#rule-priority","title":"Rule Priority","text":"<p>Rules may overlap.</p> <p>To resolve conflicts, Limify uses priority values.</p> <p>Higher priority rules take precedence.</p> <p>Example:</p> <pre><code>{\n    \"id\": \"items\",\n    \"method\": \"*\",\n    \"path\": \"/items\",\n    \"rate\": \"10/minute\",\n    \"priority\": 10,\n}\n</code></pre> <p>Example conflict:</p> <p>Rule A:</p> <pre><code>path = \"/api/**\"\npriority = 0\n</code></pre> <p>Rule B:</p> <pre><code>path = \"/api/users\"\npriority = 10\n</code></pre> <p>A request to:</p> <pre><code>/api/users\n</code></pre> <p>will match Rule B, because it has higher priority.</p>"},{"location":"core-concepts/rules/#rule-evaluation","title":"Rule Evaluation","text":"<p>When a request arrives, Limify:</p> <ol> <li>Collects all rules matching the request method and path</li> <li>Sorts matching rules by priority</li> <li>Selects the highest priority rule</li> <li>Applies the associated rate limit</li> </ol> <p>This ensures deterministic behavior even with overlapping rules.</p>"},{"location":"core-concepts/rules/#example-rule-set","title":"Example Rule Set","text":"<p>Example configuration:</p> <pre><code>rules = [\n    {\n        \"id\": \"default\",\n        \"method\": \"*\",\n        \"path\": \"/**\",\n        \"rate\": \"5/minute\",\n        \"priority\": 0,\n    },\n    {\n        \"id\": \"items\",\n        \"method\": \"*\",\n        \"path\": \"/items\",\n        \"rate\": \"10/minute\",\n        \"priority\": 10,\n    },\n]\n</code></pre> <p>Behavior:</p> <pre><code>GET /items        \u2192 10/minute\nGET /users        \u2192 5/minute\nPOST /items       \u2192 10/minute\n</code></pre> <p>The <code>items</code> rule overrides the default rule due to higher priority.</p>"},{"location":"core-concepts/rules/#best-practices","title":"Best Practices","text":""},{"location":"core-concepts/rules/#use-a-default-rule","title":"Use a default rule","text":"<p>Define a catch-all rule:</p> <pre><code>\"path\": \"/**\"\n</code></pre> <p>This ensures all endpoints have a rate limit.</p>"},{"location":"core-concepts/rules/#use-specific-rules-for-critical-endpoints","title":"Use specific rules for critical endpoints","text":"<p>Example:</p> <pre><code>/login\n/register\n/payment\n</code></pre> <p>These endpoints often require stricter limits.</p>"},{"location":"core-concepts/rules/#keep-rule-ids-stable","title":"Keep rule IDs stable","text":"<p>Changing rule IDs changes the Redis key structure, which resets rate-limit state.</p>"},{"location":"core-concepts/rules/#summary","title":"Summary","text":"<p>Rules control how rate limits apply to requests.</p> <p>They define:</p> <ul> <li>request matching (method + path)</li> <li>rate limits</li> <li>rule priority</li> </ul> <p>Combined with plans and identity resolution, rules form the foundation of Limify's rate-limiting behavior.</p>"},{"location":"getting-started/installation/","title":"Installation","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.10+</li> <li>Redis</li> </ul>"},{"location":"getting-started/installation/#install-limify","title":"Install Limify","text":"<pre><code>pip install limifyrate\n</code></pre>"},{"location":"getting-started/installation/#install-with-fastapi-adapter","title":"Install with FastAPI adapter","text":"<pre><code>pip install \"limifyrate[fastapi]\"\n</code></pre>"},{"location":"getting-started/installation/#development-installation","title":"Development Installation","text":"<pre><code>git clone https://github.com/pedram-aliniaye-asli/limify.git\ncd limify\npip install -e \".[dev]\"\n</code></pre>"},{"location":"getting-started/installation/#redis-setup","title":"Redis Setup","text":"<p>Example Redis instance:</p> <pre><code>redis-server\n</code></pre> <p>Or using Docker:</p> <pre><code>docker run -p 6379:6379 redis\n</code></pre>"},{"location":"getting-started/quickstart/","title":"Quick Start","text":"<p>This guide shows how to integrate Limify into a Python application.</p> <p>Limify supports both async and sync Redis clients.</p>"},{"location":"getting-started/quickstart/#install","title":"Install","text":"<p>Core package:</p> <pre><code>pip install limifyrate\n</code></pre> <p>With FastAPI adapter:</p> <pre><code>pip install \"limifyrate[fastapi]\"\n</code></pre>"},{"location":"getting-started/quickstart/#async-example-fastapi","title":"Async Example (FastAPI)","text":"<pre><code>import redis.asyncio as redis\n\nfrom fastapi import FastAPI\nfrom limify import Limify, LimifyConfig\nfrom limify.adapters.fastapi.middleware import LimifyMiddleware\nfrom limify.core.redis_adapter import RedisAsyncAdapter\nfrom limify.core.algorithms.token_bucket import AsyncTokenBucketAlgorithm\n\n\nredis_client = redis.from_url(\"redis://localhost:6379\")\n\nadapter = RedisAsyncAdapter(redis_client)\n\nalgorithm = AsyncTokenBucketAlgorithm(adapter)\nawait algorithm.initialize()\n\nconfig = LimifyConfig(\n    rules=[\n        {\n            \"id\": \"default\",\n            \"method\": \"*\",\n            \"path\": \"/**\",\n            \"rate\": \"5/minute\",\n        }\n    ]\n)\n\nlimify = Limify(\n    algorithm=algorithm,\n    config=config,\n)\n\napp = FastAPI()\n\napp.add_middleware(\n    LimifyMiddleware,\n    limiter=limify.limiter,\n)\n\n\n@app.get(\"/items\")\nasync def items():\n    return {\"status\": \"ok\"}\n</code></pre> <p>Requests exceeding the configured rate will return HTTP 429.</p>"},{"location":"getting-started/quickstart/#sync-example","title":"Sync Example","text":"<pre><code>import redis\n\nfrom limify import Limify, LimifyConfig\nfrom limify.core.redis_adapter import RedisSyncAdapter\nfrom limify.core.algorithms.token_bucket import TokenBucketAlgorithm\n\n\nredis_client = redis.Redis(host=\"localhost\", port=6379)\n\nadapter = RedisSyncAdapter(redis_client)\n\nalgorithm = TokenBucketAlgorithm(adapter)\n\nconfig = LimifyConfig(\n    rules=[\n        {\n            \"id\": \"default\",\n            \"method\": \"*\",\n            \"path\": \"/**\",\n            \"rate\": \"5/minute\",\n        }\n    ]\n)\n\nlimify = Limify(\n    algorithm=algorithm,\n    config=config,\n)\n</code></pre> <p>The sync version can be used in synchronous frameworks or background workers.</p>"},{"location":"getting-started/quickstart/#what-happens-internally","title":"What Happens Internally","text":"<p>For each request Limify:</p> <ol> <li>Resolves the matching rule</li> <li>Determines the active plan</li> <li>Builds an identity key</li> <li>Executes the rate-limiting algorithm</li> <li>Allows or blocks the request</li> </ol> <p>The token bucket state is stored in Redis and updated atomically using Lua scripts.</p>"},{"location":"guides/extension-guide/","title":"Extension Guide","text":"<p>Limify is designed to be extensible.</p> <p>Its architecture separates:</p> <ul> <li>framework integration</li> <li>request resolution</li> <li>rate-limiting logic</li> <li>storage persistence</li> </ul> <p>This makes it possible to extend Limify without modifying the core behavior.</p> <p>Typical extension points include:</p> <ul> <li>custom algorithms</li> <li>custom storage adapters</li> <li>custom plan providers</li> <li>custom framework adapters</li> </ul> <p>This guide focuses on the two main extension points:</p> <ul> <li>custom algorithms</li> <li>custom storage adapters</li> </ul>"},{"location":"guides/extension-guide/#extension-philosophy","title":"Extension Philosophy","text":"<p>Limify follows a clean architecture approach.</p> <p>The core idea is:</p> <ul> <li>algorithms should not depend on frameworks</li> <li>storage should not depend on business logic</li> <li>adapters should isolate external systems</li> </ul> <p>This means each extension should have a single responsibility.</p> <p>For example:</p> <ul> <li>an algorithm decides whether a request is allowed</li> <li>a storage adapter persists and retrieves state</li> <li>a framework adapter translates HTTP requests into Limify context objects</li> </ul> <p>Keeping those boundaries clear makes extensions easier to test and maintain.</p>"},{"location":"guides/extension-guide/#custom-algorithm","title":"Custom Algorithm","text":"<p>A custom algorithm is useful when Token Bucket is not the best fit for your traffic model.</p> <p>Examples of alternative algorithms:</p> <ul> <li>Sliding Window</li> <li>Fixed Window</li> <li>Leaky Bucket</li> <li>concurrency limits</li> <li>quota-based limits</li> </ul> <p>A custom algorithm should implement the same role as the built-in algorithms:</p> <ol> <li>receive the resolved key and plan</li> <li>retrieve/update state through the storage adapter</li> <li>return the rate-limit decision</li> </ol>"},{"location":"guides/extension-guide/#what-an-algorithm-needs","title":"What an Algorithm Needs","text":"<p>A Limify algorithm typically works with:</p> <ul> <li>a key that uniquely identifies the rate limit bucket</li> <li>a plan containing:</li> <li><code>id</code></li> <li><code>limit</code></li> <li><code>period_seconds</code></li> <li>a storage adapter</li> <li>the current timestamp</li> </ul> <p>The algorithm should return a result that indicates:</p> <ul> <li>whether the request is allowed</li> <li>how many requests remain</li> <li>how long until reset or retry</li> </ul> <p>This keeps the algorithm independent of frameworks and request objects.</p>"},{"location":"guides/extension-guide/#responsibilities-of-an-algorithm","title":"Responsibilities of an Algorithm","text":"<p>A custom algorithm should be responsible for:</p> <ul> <li>interpreting the rate limit values</li> <li>reading current state</li> <li>updating state</li> <li>deciding allow/block</li> <li>returning structured output</li> </ul> <p>A custom algorithm should not be responsible for:</p> <ul> <li>HTTP responses</li> <li>request parsing</li> <li>identity selection</li> <li>plan lookup</li> <li>framework-specific behavior</li> </ul> <p>Those responsibilities belong to other layers.</p>"},{"location":"guides/extension-guide/#example-custom-fixed-window-algorithm","title":"Example: Custom Fixed Window Algorithm","text":"<p>Below is a simplified conceptual example:</p> <pre><code>import time\nfrom dataclasses import dataclass\n\n\n@dataclass\nclass LimitationResult:\n    allowed: bool\n    remaining: int\n    reset_after: int\n\n\nclass FixedWindowAlgorithm:\n    def __init__(self, storage):\n        self.storage = storage\n\n    def check(self, key, plan):\n        now = int(time.time())\n        window_start = now - (now % plan.period_seconds)\n        window_key = f\"{key}:{window_start}\"\n\n        current = self.storage.get(window_key) or 0\n\n        if current &gt;= plan.limit:\n            reset_after = plan.period_seconds - (now % plan.period_seconds)\n            return LimitationResult(\n                allowed=False,\n                remaining=0,\n                reset_after=reset_after,\n            )\n\n        self.storage.increment(window_key, ttl=plan.period_seconds)\n\n        return LimitationResult(\n            allowed=True,\n            remaining=max(plan.limit - current - 1, 0),\n            reset_after=plan.period_seconds - (now % plan.period_seconds),\n        )\n</code></pre> <p>This example is intentionally simplified, but it shows the core idea:</p> <ul> <li>calculate the active window</li> <li>count requests in that window</li> <li>reject if the limit is reached</li> <li>otherwise increment the count</li> </ul>"},{"location":"guides/extension-guide/#async-custom-algorithm","title":"Async Custom Algorithm","text":"<p>For async applications, your algorithm may need async methods.</p> <p>Example shape:</p> <pre><code>class AsyncCustomAlgorithm:\n    def __init__(self, storage):\n        self.storage = storage\n\n    async def check(self, key, plan):\n        ...\n</code></pre> <p>This is similar to Limify\u2019s <code>AsyncTokenBucketAlgorithm</code>.</p>"},{"location":"guides/extension-guide/#using-the-storage-adapter","title":"Using the Storage Adapter","text":"<p>Algorithms should rely on the storage adapter instead of talking directly to Redis or another backend.</p> <p>Why:</p> <ul> <li>easier to test</li> <li>easier to replace storage backends</li> <li>cleaner architecture</li> <li>consistent interface</li> </ul> <p>For example, the built-in token bucket algorithm uses the storage adapter to:</p> <ul> <li>load a Lua script</li> <li>execute it atomically</li> <li>retrieve the updated bucket state</li> </ul> <p>Your custom algorithm should follow the same principle.</p>"},{"location":"guides/extension-guide/#testing-a-custom-algorithm","title":"Testing a Custom Algorithm","text":"<p>A custom algorithm should be tested independently from framework code.</p> <p>Typical tests should verify:</p> <ul> <li>request allowed when under limit</li> <li>request blocked when limit exceeded</li> <li>state updates correctly</li> <li>retry/reset values are correct</li> <li>storage failures are handled properly</li> </ul> <p>Because the algorithm is isolated, it should be possible to test it using:</p> <ul> <li>mocked storage adapters</li> <li>fake storage backends</li> <li>deterministic timestamps</li> </ul>"},{"location":"guides/extension-guide/#custom-storage","title":"Custom Storage","text":"<p>A custom storage adapter allows Limify to persist rate-limit state in a backend other than Redis.</p> <p>Examples of possible storage backends:</p> <ul> <li>in-memory storage</li> <li>PostgreSQL</li> <li>Memcached</li> <li>distributed cache services</li> <li>custom internal infrastructure</li> </ul> <p>Storage adapters are responsible for providing the persistence operations needed by algorithms.</p>"},{"location":"guides/extension-guide/#why-storage-is-abstracted","title":"Why Storage Is Abstracted","text":"<p>Limify algorithms should not depend on:</p> <ul> <li>Redis client details</li> <li>connection handling</li> <li>sync vs async command syntax</li> <li>backend-specific APIs</li> </ul> <p>Instead, they depend on an abstraction.</p> <p>This allows the same algorithm design to work across different storage systems.</p>"},{"location":"guides/extension-guide/#current-built-in-storage-adapters","title":"Current Built-In Storage Adapters","text":"<p>Limify currently includes:</p> <ul> <li><code>RedisSyncAdapter</code></li> <li><code>RedisAsyncAdapter</code></li> </ul> <p>These adapters provide the persistence and script execution functionality required by the built-in token bucket algorithm.</p>"},{"location":"guides/extension-guide/#responsibilities-of-a-storage-adapter","title":"Responsibilities of a Storage Adapter","text":"<p>A storage adapter should be responsible for:</p> <ul> <li>communicating with the backend</li> <li>reading and writing state</li> <li>exposing a clean interface to algorithms</li> <li>hiding backend-specific implementation details</li> </ul> <p>A storage adapter should not contain rate-limiting policy logic.</p> <p>That logic belongs in the algorithm.</p>"},{"location":"guides/extension-guide/#minimal-redis-compatible-interface","title":"Minimal Redis-Compatible Interface","text":"<p>Your current token bucket algorithm expects Redis-oriented behavior, including:</p> <ul> <li><code>script_load(...)</code></li> <li><code>evalsha(...)</code></li> </ul> <p>That is because the built-in token bucket implementation uses Lua for atomic execution.</p> <p>So if you want to support the existing token bucket algorithm with another backend, you would need an adapter that can provide equivalent behavior.</p>"},{"location":"guides/extension-guide/#example-in-memory-storage-adapter","title":"Example: In-Memory Storage Adapter","text":"<p>For simpler algorithms or testing, you can build an in-memory adapter.</p> <p>Example:</p> <pre><code>class InMemoryStorage:\n    def __init__(self):\n        self.data = {}\n\n    def get(self, key):\n        return self.data.get(key)\n\n    def set(self, key, value):\n        self.data[key] = value\n\n    def increment(self, key, ttl=None):\n        self.data[key] = self.data.get(key, 0) + 1\n        return self.data[key]\n</code></pre> <p>This is useful for:</p> <ul> <li>testing</li> <li>local development</li> <li>prototypes</li> </ul> <p>However, it is not suitable for distributed production systems, because each process would maintain separate state.</p>"},{"location":"guides/extension-guide/#matching-storage-to-algorithm","title":"Matching Storage to Algorithm","text":"<p>Not every storage adapter fits every algorithm.</p> <p>Examples:</p>"},{"location":"guides/extension-guide/#token-bucket-with-redis-lua","title":"Token Bucket with Redis Lua","text":"<p>Best suited for:</p> <ul> <li>Redis</li> <li>atomic scripting support</li> <li>distributed systems</li> </ul>"},{"location":"guides/extension-guide/#fixed-window-with-in-memory-storage","title":"Fixed Window with In-Memory Storage","text":"<p>Best suited for:</p> <ul> <li>local development</li> <li>testing</li> <li>single-process applications</li> </ul> <p>So when building a custom algorithm or adapter, think of them as a pair:</p> <ul> <li>algorithm design</li> <li>storage capability</li> </ul>"},{"location":"guides/extension-guide/#designing-a-clean-adapter-interface","title":"Designing a Clean Adapter Interface","text":"<p>If you plan to support multiple backends in the future, it may be useful to define a formal adapter protocol or base class.</p> <p>For example:</p> <pre><code>class StorageAdapter:\n    def get(self, key):\n        raise NotImplementedError\n\n    def set(self, key, value):\n        raise NotImplementedError\n\n    def increment(self, key, ttl=None):\n        raise NotImplementedError\n</code></pre> <p>Or for Redis-script-based algorithms:</p> <pre><code>class ScriptStorageAdapter:\n    def script_load(self, script: str):\n        raise NotImplementedError\n\n    def evalsha(self, sha: str, num_keys: int, *args):\n        raise NotImplementedError\n</code></pre> <p>This makes extension more predictable and easier to document.</p>"},{"location":"guides/extension-guide/#custom-framework-adapters","title":"Custom Framework Adapters","text":"<p>Although this page focuses on algorithms and storage, Limify can also be extended with new framework adapters.</p> <p>A framework adapter should:</p> <ul> <li>intercept requests</li> <li>extract method/path/client metadata</li> <li>build a <code>RequestContext</code></li> <li>call the limiter</li> <li>enforce the result in framework-specific form</li> </ul> <p>This is how the FastAPI/Starlette middleware works.</p> <p>Future adapters could target:</p> <ul> <li>Flask</li> <li>Django</li> <li>generic ASGI</li> <li>API gateway integrations</li> </ul>"},{"location":"guides/extension-guide/#best-practices-for-extensions","title":"Best Practices for Extensions","text":"<p>When extending Limify:</p>"},{"location":"guides/extension-guide/#keep-responsibilities-narrow","title":"Keep responsibilities narrow","text":"<p>Do not mix framework logic, algorithm logic, and storage code in the same component.</p>"},{"location":"guides/extension-guide/#reuse-existing-abstractions","title":"Reuse existing abstractions","text":"<p>Use <code>RequestContext</code>, <code>Plan</code>, and Limify\u2019s core flow instead of bypassing them.</p>"},{"location":"guides/extension-guide/#prefer-deterministic-behavior","title":"Prefer deterministic behavior","text":"<p>Rate limiting should behave consistently under concurrency and load.</p>"},{"location":"guides/extension-guide/#test-extensions-independently","title":"Test extensions independently","text":"<p>A custom algorithm or adapter should be testable in isolation.</p>"},{"location":"guides/extension-guide/#document-assumptions-clearly","title":"Document assumptions clearly","text":"<p>If your algorithm depends on atomic storage operations, note that explicitly.</p>"},{"location":"guides/extension-guide/#summary","title":"Summary","text":"<p>Limify is designed to be extended in a clean and predictable way.</p> <p>You can extend it by adding:</p> <ul> <li>custom algorithms</li> <li>custom storage adapters</li> <li>custom plan providers</li> <li>custom framework adapters</li> </ul> <p>A custom algorithm should focus on rate-limiting logic.</p> <p>A custom storage adapter should focus on persistence and backend communication.</p> <p>Keeping these responsibilities separate preserves Limify\u2019s architecture and makes the system easier to evolve.</p>"},{"location":"guides/release-guide/","title":"Release Guide","text":"<p>This guide explains how new versions of Limify are released.</p> <p>Limify follows semantic versioning and uses Git tags and GitHub Actions to publish packages.</p>"},{"location":"guides/release-guide/#versioning","title":"Versioning","text":"<p>Limify follows Semantic Versioning (SemVer).</p> <p>Version format:</p> <pre><code>MAJOR.MINOR.PATCH\n</code></pre> <p>Example versions:</p> <pre><code>0.1.0\n0.1.1\n0.2.0\n</code></pre>"},{"location":"guides/release-guide/#version-meaning","title":"Version Meaning","text":""},{"location":"guides/release-guide/#major-version","title":"MAJOR version","text":"<pre><code>X.0.0\n</code></pre> <p>Incremented when breaking changes are introduced.</p> <p>Examples:</p> <ul> <li>removing public APIs</li> <li>changing configuration behavior</li> <li>changing request context structure</li> </ul> <p>Example:</p> <pre><code>1.0.0\n</code></pre>"},{"location":"guides/release-guide/#minor-version","title":"MINOR version","text":"<pre><code>0.X.0\n</code></pre> <p>Incremented when new features are added without breaking existing functionality.</p> <p>Examples:</p> <ul> <li>new algorithms</li> <li>new framework adapters</li> <li>new storage adapters</li> </ul> <p>Example:</p> <pre><code>0.2.0\n</code></pre>"},{"location":"guides/release-guide/#patch-version","title":"PATCH version","text":"<pre><code>0.0.X\n</code></pre> <p>Incremented for bug fixes or small improvements.</p> <p>Examples:</p> <ul> <li>fixing algorithm edge cases</li> <li>documentation updates</li> <li>small performance improvements</li> </ul> <p>Example:</p> <pre><code>0.1.1\n</code></pre>"},{"location":"guides/release-guide/#pre-10-versions","title":"Pre-1.0 Versions","text":"<p>Before version 1.0.0, the API may still evolve.</p> <p>Example:</p> <pre><code>0.1.x\n</code></pre> <p>During this stage:</p> <ul> <li>APIs may change</li> <li>internal architecture may evolve</li> <li>documentation may expand</li> </ul> <p>However, changes will still aim to remain stable whenever possible.</p>"},{"location":"guides/release-guide/#creating-a-release","title":"Creating a Release","text":"<p>Releases are created using Git tags.</p>"},{"location":"guides/release-guide/#automated-publishing","title":"Automated Publishing","text":"<p>When a tag is pushed, the GitHub Actions workflow is triggered.</p> <p>The workflow performs the following steps:</p> <ol> <li>checks out the repository</li> <li>installs build tools</li> <li>builds the package</li> <li>runs package validation</li> <li>uploads the package to PyPI</li> </ol> <p>This means publishing happens automatically after tagging.</p>"},{"location":"guides/release-guide/#versioning_1","title":"Versioning","text":"<p>Limify follows semantic versioning.</p> <p>Example:</p> <p>0.1.0 0.1.1 0.2.0  </p>"},{"location":"guides/release-guide/#publishing","title":"Publishing","text":"<p>GitHub Actions publishes the package to PyPI.</p>"},{"location":"guides/release-guide/#pypi-package","title":"PyPI Package","text":"<p>After the workflow completes successfully, the new version becomes available on PyPI.</p> <p>Users can install it with:</p> <pre><code>pip install limifyrate\n</code></pre> <p>To upgrade:</p> <pre><code>pip install --upgrade limifyrate\n</code></pre>"},{"location":"internals/algorithms/","title":"Algorithms","text":"<p>Limify enforces rate limits using rate-limiting algorithms.</p> <p>Algorithms are responsible for deciding whether a request should be:</p> <ul> <li>allowed</li> <li>delayed</li> <li>rejected</li> </ul> <p>They operate on numeric parameters derived from rules and plans.</p> <p>Currently implemented:</p> <ul> <li>Token Bucket (sync)</li> <li>Token Bucket (async)</li> </ul> <p>Both implementations share the same logic but use different Redis clients.</p>"},{"location":"internals/algorithms/#token-bucket","title":"Token Bucket","text":"<p>The Token Bucket algorithm is a widely used rate-limiting technique that allows bursts while maintaining a controlled average request rate.</p> <p>Instead of simply counting requests per window, it models rate limiting as a bucket filled with tokens.</p> <p>Requests consume tokens from the bucket.</p> <p>If the bucket becomes empty, requests are rejected.</p>"},{"location":"internals/algorithms/#concept","title":"Concept","text":"<p>Each identity has a bucket containing tokens.</p> <p>The bucket has three parameters:</p> <pre><code>capacity       \u2192 maximum tokens in the bucket\nrefill rate    \u2192 tokens added per second\ncurrent tokens \u2192 tokens available now\n</code></pre> <p>Example configuration:</p> <pre><code>10/minute\n</code></pre> <p>Converted internally:</p> <pre><code>limit = 10\nperiod_seconds = 60\nrefill_rate = 10 / 60 = 0.1667 tokens per second\n</code></pre> <p>This means the bucket:</p> <ul> <li>can hold 10 tokens</li> <li>refills 0.1667 tokens every second</li> </ul>"},{"location":"internals/algorithms/#request-flow","title":"Request Flow","text":"<p>When a request arrives, Limify performs the following steps.</p>"},{"location":"internals/algorithms/#1-load-bucket-state","title":"1. Load bucket state","text":"<p>The current bucket state is retrieved from Redis.</p> <p>Stored format:</p> <pre><code>tokens:last_timestamp\n</code></pre> <p>Example:</p> <pre><code>7:1718654321\n</code></pre> <p>Meaning:</p> <pre><code>tokens = 7\nlast_refill_timestamp = 1718654321\n</code></pre>"},{"location":"internals/algorithms/#2-refill-tokens","title":"2. Refill tokens","text":"<p>Limify calculates how many tokens should have been refilled since the last request.</p> <p>Formula:</p> <pre><code>refill = (now - last_timestamp) * refill_rate\n</code></pre> <p>Example:</p> <pre><code>last_timestamp = 100\nnow = 110\nrefill_rate = 0.1667\n\nrefill = (110 - 100) * 0.1667 = 1.667 tokens\n</code></pre> <p>The bucket is updated:</p> <pre><code>tokens = min(capacity, tokens + refill)\n</code></pre> <p>The bucket never exceeds its maximum capacity.</p>"},{"location":"internals/algorithms/#3-consume-a-token","title":"3. Consume a token","text":"<p>If the bucket contains at least one token:</p> <pre><code>tokens &gt;= 1\n</code></pre> <p>The request is allowed and one token is consumed.</p> <pre><code>tokens = tokens - 1\n</code></pre>"},{"location":"internals/algorithms/#4-reject-request","title":"4. Reject request","text":"<p>If the bucket is empty:</p> <pre><code>tokens &lt; 1\n</code></pre> <p>The request is rejected.</p> <p>In web frameworks, this results in:</p> <pre><code>HTTP 429 Too Many Requests\n</code></pre>"},{"location":"internals/algorithms/#burst-handling","title":"Burst Handling","text":"<p>Token Bucket allows short bursts of traffic.</p> <p>Example:</p> <p>Rule:</p> <pre><code>10/minute\n</code></pre> <p>Bucket capacity:</p> <pre><code>10 tokens\n</code></pre> <p>A client may immediately send:</p> <pre><code>10 requests at once\n</code></pre> <p>But after that, the client must wait for tokens to refill.</p> <p>Refill speed:</p> <pre><code>1 token every 6 seconds\n</code></pre> <p>This behavior makes Token Bucket ideal for APIs.</p>"},{"location":"internals/algorithms/#redis-storage","title":"Redis Storage","text":"<p>Each identity has a separate bucket stored in Redis.</p> <p>Example key:</p> <pre><code>limify:items:default:user:42\n</code></pre> <p>Value format:</p> <pre><code>tokens:last_timestamp\n</code></pre> <p>Example:</p> <pre><code>4:1718654400\n</code></pre> <p>This means:</p> <pre><code>tokens = 4\nlast_refill_timestamp = 1718654400\n</code></pre>"},{"location":"internals/algorithms/#redis-lua-execution","title":"Redis Lua Execution","text":"<p>Limify executes the token bucket logic using Redis Lua scripts.</p> <p>Example operations performed in the script:</p> <pre><code>1. read current bucket state\n2. calculate token refill\n3. update token count\n4. store new state\n5. return remaining tokens\n</code></pre> <p>The script receives arguments:</p> <pre><code>ARGV[1] \u2192 limit\nARGV[2] \u2192 period_seconds\nARGV[3] \u2192 current timestamp\n</code></pre> <p>From these values it computes:</p> <pre><code>refill_rate = limit / period_seconds\n</code></pre>"},{"location":"internals/algorithms/#why-lua-is-used","title":"Why Lua Is Used","text":"<p>Lua scripts allow the entire rate-limiting process to run inside Redis.</p> <p>Benefits:</p>"},{"location":"internals/algorithms/#atomic-execution","title":"Atomic execution","text":"<p>All calculations and updates happen in a single Redis operation.</p> <p>This prevents race conditions when multiple servers process requests simultaneously.</p>"},{"location":"internals/algorithms/#distributed-safety","title":"Distributed safety","text":"<p>Multiple API servers can share the same Redis instance without corrupting bucket state.</p>"},{"location":"internals/algorithms/#high-performance","title":"High performance","text":"<p>Instead of multiple Redis commands:</p> <pre><code>GET\nCALCULATE\nSET\n</code></pre> <p>Lua executes everything in one atomic operation.</p>"},{"location":"internals/algorithms/#sync-vs-async-algorithms","title":"Sync vs Async Algorithms","text":"<p>Limify provides two implementations.</p>"},{"location":"internals/algorithms/#asynctokenbucketalgorithm","title":"AsyncTokenBucketAlgorithm","text":"<p>Uses <code>redis.asyncio</code> and is intended for asynchronous frameworks such as:</p> <ul> <li>FastAPI</li> <li>Starlette</li> <li>async workers</li> </ul> <p>Example initialization:</p> <pre><code>algorithm = AsyncTokenBucketAlgorithm(redis_adapter)\nawait algorithm.initialize()\n</code></pre>"},{"location":"internals/algorithms/#tokenbucketalgorithm","title":"TokenBucketAlgorithm","text":"<p>Uses the synchronous Redis client and works with synchronous applications.</p> <p>Example:</p> <pre><code>algorithm = TokenBucketAlgorithm(redis_adapter)\n</code></pre> <p>Both versions use the same Lua script and enforce identical behavior.</p>"},{"location":"internals/algorithms/#why-token-bucket-was-chosen","title":"Why Token Bucket Was Chosen","text":"<p>Token Bucket provides several advantages:</p>"},{"location":"internals/algorithms/#burst-tolerance","title":"Burst tolerance","text":"<p>Allows short bursts of requests.</p>"},{"location":"internals/algorithms/#smooth-refill-behavior","title":"Smooth refill behavior","text":"<p>Limits traffic without harsh fixed windows.</p>"},{"location":"internals/algorithms/#efficient-implementation","title":"Efficient implementation","text":"<p>Requires minimal storage and simple calculations.</p>"},{"location":"internals/algorithms/#widely-adopted","title":"Widely adopted","text":"<p>Used by systems such as:</p> <ul> <li>NGINX</li> <li>Envoy</li> <li>API gateways</li> <li>cloud rate limiters</li> </ul>"},{"location":"internals/algorithms/#future-algorithms","title":"Future Algorithms","text":"<p>Future versions of Limify may include additional algorithms such as:</p> <pre><code>Sliding Window\nFixed Window\nLeaky Bucket\n</code></pre> <p>These algorithms serve different traffic patterns and system requirements.</p>"},{"location":"internals/algorithms/#summary","title":"Summary","text":"<p>Limify currently uses the Token Bucket algorithm to enforce rate limits.</p> <p>The algorithm:</p> <ol> <li>refills tokens over time</li> <li>consumes tokens per request</li> <li>rejects requests when tokens are empty</li> </ol> <p>Bucket state is stored in Redis and updated using atomic Lua scripts, ensuring safe operation in distributed systems.</p>"},{"location":"internals/framework-adapters/","title":"Framework Adapters","text":"<p>Framework adapters integrate Limify with web frameworks.</p> <p>They connect the framework request lifecycle with Limify\u2019s rate-limiting engine.</p> <p>Currently supported adapters:</p> <ul> <li>FastAPI</li> <li>Starlette</li> </ul> <p>These adapters are implemented as middleware.</p>"},{"location":"internals/framework-adapters/#what-framework-adapters-do","title":"What Framework Adapters Do","text":"<p>Framework adapters are responsible for:</p> <ol> <li>Intercepting incoming requests</li> <li>Extracting request metadata</li> <li>Constructing a <code>RequestContext</code></li> <li>Calling the Limify limiter</li> <li>Blocking requests when limits are exceeded</li> <li>Adding rate limit headers to responses</li> </ol> <p>The adapter layer allows Limify to remain framework-agnostic.</p> <p>The core Limify engine does not depend on any specific web framework.</p>"},{"location":"internals/framework-adapters/#middleware-integration","title":"Middleware Integration","text":"<p>Framework adapters are implemented as middleware components.</p> <p>Middleware sits between the HTTP server and your application endpoints.</p> <p>Request flow:</p> <pre><code>Client Request\n      \u2193\nFramework Middleware (Limify)\n      \u2193\nLimiter\n      \u2193\nApplication Endpoint\n</code></pre> <p>If a request exceeds its rate limit, the middleware immediately returns an error response.</p>"},{"location":"internals/framework-adapters/#fastapi-adapter","title":"FastAPI Adapter","text":"<p>Limify provides a FastAPI-compatible middleware:</p> <pre><code>limify.adapters.fastapi.middleware.LimifyMiddleware\n</code></pre> <p>Example usage:</p> <pre><code>from fastapi import FastAPI\nfrom limify.adapters.fastapi.middleware import LimifyMiddleware\n\napp = FastAPI()\n\napp.add_middleware(\n    LimifyMiddleware,\n    limiter=limify.limiter,\n)\n</code></pre> <p>Once installed, all requests passing through the application will be evaluated by Limify.</p>"},{"location":"internals/framework-adapters/#starlette-compatibility","title":"Starlette Compatibility","text":"<p>FastAPI is built on top of Starlette, so the middleware is compatible with both frameworks.</p> <p>This means Limify can work with:</p> <ul> <li>FastAPI</li> <li>Starlette</li> <li>other ASGI frameworks that support Starlette-style middleware</li> </ul>"},{"location":"internals/framework-adapters/#requestcontext-construction","title":"RequestContext Construction","text":"<p>When a request arrives, the middleware constructs a <code>RequestContext</code> object.</p> <p>Example:</p> <pre><code>RequestContext(\n    method=\"GET\",\n    path=\"/items\",\n    user_id=None,\n    org_id=None,\n    api_key=None,\n    client_ip=\"127.0.0.1\"\n)\n</code></pre> <p>The context contains the information needed for:</p> <ul> <li>rule matching</li> <li>identity resolution</li> <li>plan resolution</li> </ul> <p>Framework adapters are responsible for extracting this information from the request.</p>"},{"location":"internals/framework-adapters/#rate-limit-enforcement","title":"Rate Limit Enforcement","text":"<p>The middleware sends the request context to the Limify limiter:</p> <pre><code>limiter.check(context)\n</code></pre> <p>The limiter then:</p> <ol> <li>resolves the matching rule</li> <li>resolves the applicable plan</li> <li>resolves the request identity</li> <li>executes the rate-limiting algorithm</li> </ol> <p>If the request is allowed, the middleware forwards it to the application endpoint.</p> <p>If the request exceeds its limit, the middleware immediately returns an error response.</p>"},{"location":"internals/framework-adapters/#rate-limit-headers","title":"Rate Limit Headers","text":"<p>Limify adds standard rate limit headers to responses.</p> <p>These headers help clients understand their current rate limit status.</p>"},{"location":"internals/framework-adapters/#x-ratelimit-limit","title":"X-RateLimit-Limit","text":"<p>The maximum number of requests allowed in the current period.</p> <p>Example:</p> <pre><code>X-RateLimit-Limit: 100\n</code></pre>"},{"location":"internals/framework-adapters/#x-ratelimit-remaining","title":"X-RateLimit-Remaining","text":"<p>The number of requests remaining before the rate limit is reached.</p> <p>Example:</p> <pre><code>X-RateLimit-Remaining: 42\n</code></pre>"},{"location":"internals/framework-adapters/#retry-after","title":"Retry-After","text":"<p>Indicates how long the client should wait before making another request.</p> <p>Example:</p> <pre><code>Retry-After: 12\n</code></pre> <p>This value is returned when the rate limit has been exceeded.</p>"},{"location":"internals/framework-adapters/#rate-limit-response","title":"Rate Limit Response","text":"<p>If a request exceeds its rate limit, the middleware returns an HTTP error.</p> <p>Typical response:</p> <pre><code>HTTP/1.1 429 Too Many Requests\n</code></pre> <p>Example response body:</p> <pre><code>{\n  \"detail\": \"Rate limit exceeded\"\n}\n</code></pre> <p>The <code>Retry-After</code> header indicates when requests can resume.</p>"},{"location":"internals/framework-adapters/#example-flow","title":"Example Flow","text":"<p>A request arrives:</p> <pre><code>GET /items\n</code></pre> <p>Middleware steps:</p> <ol> <li>Build <code>RequestContext</code></li> <li>Call limiter</li> <li>Execute token bucket algorithm</li> <li>Receive result</li> </ol> <p>If allowed:</p> <pre><code>\u2192 Request proceeds to endpoint\n</code></pre> <p>If blocked:</p> <pre><code>\u2192 HTTP 429 returned\n</code></pre>"},{"location":"internals/framework-adapters/#why-framework-adapters-matter","title":"Why Framework Adapters Matter","text":"<p>Framework adapters allow Limify to support multiple environments without modifying core logic.</p> <p>Benefits:</p>"},{"location":"internals/framework-adapters/#framework-independence","title":"Framework independence","text":"<p>Core rate-limiting logic remains separate from framework code.</p>"},{"location":"internals/framework-adapters/#easy-integration","title":"Easy integration","text":"<p>Middleware installation requires only a few lines of code.</p>"},{"location":"internals/framework-adapters/#consistent-behavior","title":"Consistent behavior","text":"<p>All supported frameworks use the same rate-limiting engine.</p>"},{"location":"internals/framework-adapters/#future-framework-adapters","title":"Future Framework Adapters","text":"<p>Future versions of Limify may include adapters for additional frameworks:</p> <pre><code>Flask\nDjango\nASGI middleware (generic)\nAPI gateway integrations\n</code></pre> <p>Because Limify separates adapters from core logic, adding new frameworks is straightforward.</p>"},{"location":"internals/framework-adapters/#summary","title":"Summary","text":"<p>Framework adapters connect Limify with web frameworks.</p> <p>They:</p> <ul> <li>intercept requests</li> <li>construct request contexts</li> <li>call the rate limiter</li> <li>enforce limits</li> <li>add rate limit headers</li> </ul> <p>Currently supported adapters:</p> <pre><code>FastAPI\nStarlette\n</code></pre> <p>These adapters allow Limify to integrate seamlessly into modern Python web applications.</p>"},{"location":"internals/storage-adapters/","title":"Storage Adapters","text":"<p>Storage adapters provide the persistence layer used by rate-limiting algorithms.</p> <p>They are responsible for storing and retrieving the token bucket state.</p> <p>Currently supported adapters:</p> <ul> <li><code>RedisAsyncAdapter</code></li> <li><code>RedisSyncAdapter</code></li> </ul> <p>Both adapters interact with Redis but expose a simplified interface to the algorithm layer.</p>"},{"location":"internals/storage-adapters/#why-storage-adapters-exist","title":"Why Storage Adapters Exist","text":"<p>Limify separates rate-limiting logic from data storage.</p> <p>This design follows the principles of clean architecture.</p> <p>The algorithm should not need to know:</p> <ul> <li>which database is used</li> <li>how commands are executed</li> <li>whether the client is synchronous or asynchronous</li> </ul> <p>Instead, the algorithm communicates with a storage adapter that abstracts these details.</p> <p>This allows Limify to:</p> <ul> <li>support multiple storage backends</li> <li>support both sync and async execution</li> <li>remain framework independent</li> <li>simplify testing</li> </ul>"},{"location":"internals/storage-adapters/#redis-based-storage","title":"Redis-Based Storage","text":"<p>Limify currently uses Redis as the storage backend.</p> <p>Redis is well suited for rate limiting because it provides:</p> <ul> <li>extremely low latency</li> <li>atomic operations</li> <li>Lua scripting support</li> <li>distributed access across multiple application instances</li> </ul> <p>Each identity's rate limit state is stored in a Redis key.</p> <p>Example key:</p> <pre><code>limify:items:default:user:42\n</code></pre> <p>Value stored in Redis:</p> <pre><code>tokens:last_timestamp\n</code></pre> <p>Example:</p> <pre><code>7:1718654321\n</code></pre> <p>Meaning:</p> <pre><code>tokens = 7\nlast_refill_timestamp = 1718654321\n</code></pre>"},{"location":"internals/storage-adapters/#redisasyncadapter","title":"RedisAsyncAdapter","text":"<p><code>RedisAsyncAdapter</code> is designed for asynchronous Python applications.</p> <p>It works with the <code>redis.asyncio</code> client.</p> <p>Typical environments:</p> <ul> <li>FastAPI</li> <li>Starlette</li> <li>async workers</li> <li>asyncio-based services</li> </ul> <p>Example usage:</p> <pre><code>import redis.asyncio as redis\nfrom limify.core.redis_adapter import RedisAsyncAdapter\n\nredis_client = redis.from_url(\"redis://localhost:6379\")\n\nadapter = RedisAsyncAdapter(redis_client)\n</code></pre> <p>The async adapter exposes coroutine-based operations used by the algorithm.</p> <p>Example operations:</p> <pre><code>script_load\nevalsha\n</code></pre> <p>These methods allow the algorithm to execute Lua scripts asynchronously.</p>"},{"location":"internals/storage-adapters/#redissyncadapter","title":"RedisSyncAdapter","text":"<p><code>RedisSyncAdapter</code> is designed for synchronous Python applications.</p> <p>It works with the standard Redis client.</p> <p>Typical environments:</p> <ul> <li>Flask</li> <li>Django</li> <li>background workers</li> <li>CLI applications</li> </ul> <p>Example usage:</p> <pre><code>import redis\nfrom limify.core.redis_adapter import RedisSyncAdapter\n\nredis_client = redis.Redis(host=\"localhost\", port=6379)\n\nadapter = RedisSyncAdapter(redis_client)\n</code></pre> <p>The synchronous adapter exposes the same interface but executes Redis commands synchronously.</p>"},{"location":"internals/storage-adapters/#adapter-responsibilities","title":"Adapter Responsibilities","text":"<p>Storage adapters are responsible for:</p>"},{"location":"internals/storage-adapters/#lua-script-loading","title":"Lua script loading","text":"<p>The token bucket algorithm loads its Lua script into Redis:</p> <pre><code>SCRIPT LOAD\n</code></pre> <p>This returns a script SHA identifier used for future execution.</p>"},{"location":"internals/storage-adapters/#script-execution","title":"Script execution","text":"<p>Adapters execute the script using:</p> <pre><code>EVALSHA\n</code></pre> <p>Arguments passed to the script include:</p> <pre><code>limit\nperiod_seconds\ntimestamp\n</code></pre> <p>The script performs all rate limit calculations atomically.</p>"},{"location":"internals/storage-adapters/#redis-interaction-abstraction","title":"Redis interaction abstraction","text":"<p>Instead of the algorithm calling Redis directly, it calls adapter methods such as:</p> <pre><code>script_load(...)\nevalsha(...)\n</code></pre> <p>This abstraction makes the algorithm independent of the Redis client implementation.</p>"},{"location":"internals/storage-adapters/#why-not-call-redis-directly","title":"Why Not Call Redis Directly?","text":"<p>Without adapters, the algorithm would need to know:</p> <ul> <li>Redis client type</li> <li>async vs sync execution</li> <li>command syntax</li> <li>connection handling</li> </ul> <p>Adapters isolate this complexity.</p> <p>This improves:</p> <ul> <li>maintainability</li> <li>testability</li> <li>extensibility</li> </ul>"},{"location":"internals/storage-adapters/#example-flow","title":"Example Flow","text":"<ol> <li>Limify receives a request</li> <li>The limiter resolves the rule, plan, and identity</li> <li>The algorithm calls the storage adapter</li> <li>The adapter executes the Lua script in Redis</li> <li>Redis updates the token bucket state</li> <li>The algorithm returns the rate-limit decision</li> </ol>"},{"location":"internals/storage-adapters/#future-storage-adapters","title":"Future Storage Adapters","text":"<p>The adapter architecture allows new storage backends to be implemented easily.</p> <p>Potential future adapters:</p> <pre><code>InMemoryAdapter\nPostgreSQLAdapter\nMemcachedAdapter\nDistributed cache systems\n</code></pre> <p>As long as the adapter implements the expected interface, it can be used by Limify algorithms.</p>"},{"location":"internals/storage-adapters/#summary","title":"Summary","text":"<p>Storage adapters provide the persistence layer for Limify.</p> <p>They:</p> <ul> <li>store rate limit state</li> <li>execute Redis Lua scripts</li> <li>abstract Redis client details</li> <li>support both sync and async execution</li> </ul> <p>Currently supported adapters:</p> <pre><code>RedisAsyncAdapter\nRedisSyncAdapter\n</code></pre> <p>This design keeps the algorithm layer clean and allows Limify to support multiple storage systems in the future.</p>"}]}