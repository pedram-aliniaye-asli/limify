# Plans

Limify supports SaaS-style plans.

Example:

```
free → 10/minute
pro → 100/minute
enterprise → 1000/minute
```

If no custom plan provider is used, Limify derives the plan from the rule rate.
